# Janux — Project Review Gaps

## Status snapshot (2026-09-08)

| Tier | Closed | Partial | Open |
|---|---|---|---|
| Legacy tracker (G-28…G-127, reconciled by this review) | 13 — G-87 (by design), G-88, G-89, G-90, G-91, G-92, G-94, G-97, G-98, G-109, G-110, G-111, G-123 | 3 — G-28, G-122, G-127 | 12 — G-93, G-96, G-99, G-100, G-101, G-105, G-106, G-112, G-121, G-124, G-125, G-126 |
| New — High (G-128…G-139) | 11 | — | 1 — **G-138** (no TOTP UI) |
| New — Medium (G-140…G-166) | 12 — G-140, G-141, G-142, G-147, G-148 (by design), G-151, G-152, G-154, G-158, G-159, G-160, G-164 | 2 — G-149, G-150 | 13 — G-143, G-144, G-145, G-146, G-153, G-155, G-156, G-157, G-161, G-162, G-163, G-165, G-166 |
| New — Low (unnumbered tail) | — | — | all open |

Fix waves, each verified green at landing (final state: **319 lib + 94 unit + 31 integration + 33 HTTP e2e + 60 conformance**, all single-threaded; clippy `-D warnings`; frontend build/lint/tests):
- **2026-09-07** — G-94 (acr vocabulary) · G-128 (roles on refresh) · G-129 (policy_create escalation) · G-130+G-123+G-97 (machine-token kill chain) · G-132+G-139 (sudo mode + HttpOnly cookie session) · G-131+G-133+G-134 (out-of-box breakage).
- **2026-09-08** — G-135+G-136+G-137+G-158+G-159+G-164 (release/test integrity) + G-88 (backup/restore) · G-140+G-141+G-142+G-147+G-151+G-152+G-154+G-160 (medium security cluster) + G-149/G-150 (hardening, partial) · G-90 (client-selectable capped token lifetimes — no configuration, by owner decision) · G-89 (audit trail: actor + target attribution across all 43 audited mutation/auth handlers; v2 residuals filed as G-166) · G-148 (closed by design — domain ownership is an operator trust decision, DESIGN.md §5) · G-87 (closed by design — ephemeral ceremony state fails closed; not worth persisting).

Operator action still outstanding (G-134 residual): rotate/remove the dead provider secrets sitting in the local gitignored `.env`.

---

Fresh review conducted 2026-09-07 on the working tree @ `0d0c780`. Supersedes the deleted `gaps.md` (review of 2026-09-06) and `KNOWN_ISSUES.md` (G-* tracker, last ID G-127); G-* numbering below continues from that tracker. Method: five parallel deep reviews — OIDC/OAuth surface, passwordless factors & user lifecycle, tenancy/RBAC/SCIM/ops, frontend, tests/CI/docs — with every High finding re-verified directly in code. Findings are code-verified only; no test-run snapshot is claimed.

> **Note on G-\* citations (G-160):** code comments and docs also cite IDs from the *historical* tracker (G-1 … G-127 — e.g. G-43 in `seed.example.toml`, G-117 in `src/oidc.rs`). Those items were all closed before this review; their register (`KNOWN_ISSUES.md`) was deleted on 2026-09-07 and is recoverable from git history (`git log --diff-filter=D -- KNOWN_ISSUES.md`). Citations are kept in place as fix-provenance markers; this file is the live registry for everything G-87 onward.

---

## Legacy tracker reconciliation (G-28 … G-127)

### Closed since the last tracker (verified in code)

| ID | Evidence |
|---|---|
| G-98 PKCE `plain` accepted | Rejected outright; S256-only, constant-time compare, fail-closed exchange (`src/oidc.rs:1010-1063`, `:2488-2499`); discovery advertises `["S256"]` (`:811`) |
| G-91 health/readiness/metrics stubs | Real distinct live/ready probes + Prometheus registry, 10 collectors, bounded label cardinality (`src/ops.rs:36-75,116-242`); Docker/compose healthchecks wired. Residual: readiness checks only data-dir presence; `/admin/metrics` requires an admin JWT (no unauthenticated scrape port) |
| G-110 redirect_uri enforced client-side only | Server-side allowlist at `/authorize` before any session/consent step + re-validation at `/token` against registration and parked callback (`src/oidc.rs:982-1007`, `:2361-2386`); `post_logout_redirect_uri` exact-matched (`src/oidc_ext.rs:655-693`) |
| G-111 cookie attributes client-chosen | HttpOnly/Secure/SameSite=Strict/Path=/ hardcoded server-side at all four session-cookie sites (`src/email.rs:620-627`, `src/otp.rs:520-527`, `src/totp.rs:565-572`, `src/passkey.rs:340-347`); social bind cookie HttpOnly+Secure+Lax. Cookie *name* still client-chosen — documented as intentional (DESIGN.md:72) |
| G-109 seed test depends on gitignored files | Test falls back to tracked `base.example`/`seed.example` when `seed.toml` is absent (`src/seed.rs:183-189`). Residual: a local `seed.toml` shadows the example, so local and CI validate different inputs |
| G-92 no RP-initiated/back-channel logout (mostly) | `GET|POST /end_session` with `id_token_hint` validation, exact-matched post-logout redirects, presented-session revocation (`src/oidc_ext.rs:620-735`); back-channel fan-out from both logout paths with spec-correct events-JWT shape (`src/jwt.rs:118-150`, pinned by `logout_token_round_trip_matches_spec_shape`). No `sid`/session-management/front-channel — deliberate, advertised as such (`src/oidc.rs:800-801`, DESIGN.md:69-72). Durability residual = G-126 |

### Still open (confirmed unchanged)

- ~~**G-88** — No backup/restore/DR story. Only mechanism is the delete-time tenant snapshot (retention 5) inside `delete_tenant` (`src/db.rs:665-731`); no scheduled backup, no restore tooling, no docs. The data dir holds every tenant schema, signing keys, and the revocation store.~~ **(fixed 2026-09-08)** `janux backup <dest>` / `janux restore <src> [--force]` CLI subcommands (clap, handled before anything opens the data dir): backup is a validated COLD copy — every `*.db` under the data dir must answer a `BEGIN IMMEDIATE`/`ROLLBACK` lock probe first, which fails loudly ("is the server still running?") instead of copying torn files — then the whole tree (tenant schemas, `jwt.db` revocation store, delete-time snapshots, ACME state) is copied into `dest/backup-<unix-ts>/` with a versioned `manifest.json` (tenants + file list). Restore validates the manifest, refuses a non-empty data dir without `--force` (disaster recovery, not merge), and the restored tree boots as a working Storage. README gained a "Backup & restore" section stating the complete restore set: backup dir + config files + the `encryption_key` (without the key the at-rest secrets are unrecoverable); `just backup` wraps the common case. Test: `backup_and_restore_round_trip` (manifest shape, refuse-merge, restore boots with the original user data). Residuals: no scheduling/retention automation (cron/systemd is the operator's), backups are unencrypted-at-rest-as-a-whole (individual secrets inside already are; the `encryption_key` itself lives in config), and no off-site/replication story.
- ~~**G-89** — Audit trail is an access log: method/URI/status/duration/request_id only — no actor, tenant, target user, or old→new state; attached only to mutating routes; admin mutations record no who/what/when anywhere (`src/audit.rs:5-52`, `src/router.rs:227-382`).~~ **(fixed 2026-09-08)** The trail now answers who / what / to-whom / where / when / outcome on every audited route. First wave (earlier the same day): the hoop reads the injected session after the handler runs and attributes `actor` (username), `sub` (principal uuid), `domain` (tenant). Second wave (this fix): **target attribution** — `audit::record_target[_detail]` lets handlers record what the request acted on (`AuditTarget {kind, name, detail}` carried on the response extensions, since handlers typically hold the depot borrow for `ServerState`), rendered as `target=kind:name` + `detail=…`. Recorded BEFORE the action is attempted, so denied and failed attempts are attributed too — the level-gate 403s, sudo refusals, and ownership conflicts all show who tried what against whom. Wired across the whole audited mutation surface (43 handlers): admin user/role/policy/domain/tenant/client/key/provider CRUD (with old→new-style details: `level=79`, `active=false`, `role=…`, `grants=…`, `tenant=…`), every credential mutation (`credential:email:…`/`mobile:…`/`totp:…`/`passkey`/`social:<provider>` with user + flow), every authentication event including FAILURES (`auth:<claimed-username>` with `factor=email|otp|passkey|totp|social`, add-flow variants, logout with the decoded owner), and SCIM create/put/patch/delete (`scim-user:<id>`). G-140's query redaction and the actor fields ride the same log line. Tests: `audit_target_reads_the_recorded_context` plus `audit_hoop_renders_actor_and_target` — a real request through the hoop with a captured tracing writer, asserting `actor=`, `sub=`, `domain=`, `target=user:alice`, `detail=active=false` all reach the sink. Residuals filed as **G-166**.
- ~~**G-90** — All token lifetimes are compile-time constants (session 15 min, OIDC access 60 min, ID 15 min, refresh family 30 d, client_credentials 90 d, device 1800 s); nothing per-tenant/per-client configurable (`src/utils.rs:820`, `src/oidc.rs:398,2266`, `src/server.rs:300-318`).~~ **(fixed 2026-09-08 — by owner decision, without configuration)** Lifetimes are now **client-selectable within server ceilings**: an optional `lifetime` parameter (seconds) on `/token` (all four grants — access tokens, ceiling 60 min; `client_credentials`, ceiling 90 d) and on the session-minting ceremonies (`email/otp/passkey/totp verify`, `social` request as a query param; ceiling 15 min). `clamped_token_lifetime_minutes` (`src/utils.rs`) clamps into `[60 s, ceiling]` at minute granularity: a caller may **shorten** its tokens, never lengthen them past policy — an uncapped client-chosen lifetime would let a stolen ceremony mint a year-long session and would break the kill-chain math keyed to the ceilings (G-123's marker TTL, G-151's chain cap). `expires_in` and the introspection cache `exp` reflect the actual lifetime; shortened internal sessions **keep** their lifetime across `auth/refresh` rotation (re-mint uses the presented token's `exp − iat`, clamped). ID tokens (15-min authentication assertions), refresh-family windows (30 d), and device-flow timeouts (1800 s) stay fixed by design. The owner explicitly declined config-file/per-tenant ceilings (2026-09-08): the constants remain the single policy point, and per-client *registered* lifetimes (OAuth2Client column) remain a possible future refinement if ceilings ever need to differ per RP. Tests: `lifetime_requests_clamp_into_policy_ceiling` (None/under/at/over ceiling, sub-minute floor, negatives), `client_credentials_honors_clamped_lifetime` (1 h honored end-to-end incl. `expires_in` + decoded `exp−iat`; absurd request clamped to 90 d; omitted → ceiling), `refresh_preserves_client_shortened_lifetime` (5-min chain rotates at 5 min; default chain unchanged), `verify_honors_clamped_session_lifetime` (magic-link session minted at 300 s). OpenAPI client regenerated; INTEGRATION.md + FRONTEND.md document the parameter.
- **G-93** — `/authorize` ignores `prompt` and `max_age`; no `prompt_values_supported` in discovery; `auth_time` is carried faithfully so RPs can evaluate max_age after the fact, but the OP never enforces it (`src/oidc.rs:837-851`, `:1165-1174`).
- ~~**G-94** — `acr` vocabulary mismatch: discovery emits factor names (`email`/`otp`/`social`/`passkey`, `src/oidc.rs:734-818`), tokens emit `"1"`/`"2"` (`src/db.rs:80-89`). Nothing reconciles them.~~ **(fixed 2026-09-07)** Tokens now emit the discovery factor-name vocabulary: `acr_value` reports the *strongest factor achieved* (documented order `passkey > totp > otp > email > social`), mapping internal labels to external names (`oauth2`/legacy `Social` → `social`) with a deterministic first-sorted fallback for unknown labels so a non-empty factor set never loses the claim (`src/db.rs:80-110`). The canonical step-up session `{email, totp}` reports `"totp"`, preserving the old `"2"` = MFA semantics; the policy engine reads `mfa` directly (`src/policy.rs:188`) and `amr` still carries the full RFC 8176 method list, so nothing downstream depended on the numeric levels. Discovery now also advertises `"totp"` for provisioned tenants (`src/oidc.rs:776-786`) — step-up is always available there (session-gated, no provider config), so `acr_values_supported` covers every value a token can emit; the unprovisioned-host `[]` assertions are untouched. Tests: `tests/unit/amr_unit.rs` acr cases rewritten (single-factor names, strongest-wins multi-factor, legacy `Social`, unknown-label fallback) plus round-trip expectations; device-flow test derives its expectation via `acr_value` and adapts automatically. Verified: `cargo test --lib --test unit_tests -- --test-threads=1` → 289 + 90 passed, `cargo clippy --all-targets -- -D warnings` clean.
- ~~**G-97** — Signing-key deletion invalidates every outstanding token carrying that `kid`; no retire/publish state, no last-key guard, arbitrary "active key" re-seat (`src/key.rs:138-165`, `src/jwt.rs:193-197`). Rotation = create-then-delete = forced domain-wide logout.~~ **(fixed 2026-09-07)** Keys gained a `retired` state (new column + idempotent `ALTER TABLE keys` micro-migration in `connect_tenant`, mirroring the M3 `emails.verified` precedent — legacy rows come back signable). Rotation lifecycle is now create → **retire** → drain → delete: `key_retire` stops signing (the key is never seated in `active_key_cache`/`current_key`, and retiring the seated key re-lands the seat on a signable sibling) while the row stays resolvable by `kid` and published in the JWKS so outstanding tokens verify until natural expiry; `key_delete` **refuses active keys** ("retire it first") so the domain-wide-logout accident is no longer reachable, and deletion of a retired key is the deliberate post-drain act. Last-signable-key guard: retirement refuses when no signable sibling exists ("create its replacement first"), so a domain can never lose its ability to mint. Surface: `POST /api/v1/admin/key/retire` (same H9 cross-domain guard as delete, actionable 400 messages on both endpoints), `KeyEntry.retired` in the API, `STANDARD_ADMIN_POLICIES` + both seed files + `tests/common.rs` mirror row, admin console keys tab shows Signing/Retired status with a Retire→Delete button flow, OpenAPI client regenerated. Tests: `key_retire_drains_then_deletes` (active refuses delete; pre-retirement token still verifies through `jwt_decode` after retirement; idempotent retire; retired deletes), `key_retire_refuses_the_last_signable_key` (+ seat re-landing), `key_delete_refuses_cross_domain_keys` extended (cross-domain retire 403, retire-then-delete happy path), `legacy_tenant_db_gains_key_retired_column_on_reload`. Residual (deliberate): deleting a retired key still invalidates any token that outlived the drain — an explicit, informed operator act; JWKS cross-domain publication remains a separate Low finding.
- **G-99** — Signup gating declared in DESIGN.md:23 but no config key, no policy path (auth endpoints run under the policy-free `session` hoop), provisioning unconditional in every bootstrap factor (`src/email.rs:592-600`, `src/otp.rs:491-499`, `src/social.rs:396-422`).
- **G-100** — DESIGN.md:22 still lists passkey as bootstrap-capable; code requires an existing same-user session (`src/passkey.rs:469-483`). INTEGRATION.md:144 documents the real behavior — DESIGN.md was never reconciled.
- **G-101** — No account recovery path. No backup/recovery codes; passkey deactivation is self-session-only and there is **no admin endpoint to deactivate another user's passkeys** (`src/router.rs:161-173`); admin can strip credentials but cannot attach a replacement first one (see G-131).
- **G-105** — No identifier normalization/validation: email stored/looked up verbatim (only the *throttle* key is lowercased, `src/email.rs:437` vs `:126`); the session-gated add flow *does* lowercase (`:872`) — add and login disagree on casing. Mobile not E.164-validated (`+8613800000000` ≠ `13800000000`). No NFKC/IDNA/punycode anywhere. Usernames get no charset validation.
- **G-106** — No session visibility or global revocation: no endpoint lists or kills a user's sessions; deactivation explicitly leaves issued tokens alive (`src/db.rs:274-281`); compounds with G-128/G-151.
- **G-112** — Zero security response headers on hosted pages: no CSP, X-Frame-Options/frame-ancestors, X-Content-Type-Options, HSTS, Referrer-Policy anywhere in `src/` or `frontend/`; `pages::prepare` sets only Content-Type + ETag (`src/pages.rs:151-160`). The built HTML is CSP-ready (no inline scripts/styles), so login/consent pages are framable today.
- **G-121** — Revocation "insert-wins" commit point still not atomic: `cache.insert` is moka `entry().or_insert()` — verified look-then-insert against moka 0.12.15 source; two racers on an absent key can both observe absence (`src/cache.rs:34-41`, `src/jwt.rs:342-373`). Masked in-process by the tenant write guard; the DB-failure rollback can poison a family over a transient error.
- **G-124** — SCIM never verified against a live external IdP/conformance suite: the Python SCIM track covers discovery docs + unauthenticated 401 only; the conformance tenant seeds no `scim` client/policies so black-box provisioning *cannot* run (`tests/compliant/harness/config.py:9-35`); no Rust HTTP test touches `/scim/v2/*`. In-process lib tests are strong (`src/scim.rs:1018-1244`) but that is not external verification.
- **G-125** — RFC 7592 read-only: only `POST /register` + `GET /register/{client_id}`; no PUT/PATCH/DELETE; no `registration_access_token`/`registration_client_uri` issued (`src/router.rs:452-456`, `src/oidc_ext.rs:152-171,385-466`).
- **G-126** — Back-channel logout delivery: detached `tokio::spawn`, 3 attempts, 2s/4s in-memory backoff, nothing persisted; restart or RP outage > ~16 s drops the notification permanently (logout tokens expire at 120 s); delivery sequential per target — one slow RP delays all others (`src/oidc_ext.rs:537-574`).

### Partially fixed

- ~~**G-87** — Single-instance ceremony state: process-local magic links/OTP/challenges/rate limits, no horizontal scaling. (Partially fixed earlier: the revocation store became a persistent `jwt.db` singleton with cache read-through — logout/rotation revocations survive restarts and propagate between instances sharing the data dir, `src/jwt.rs:229-234,381-413`.)~~ **(closed 2026-09-08 — by design, owner decision)** The remaining process-local state is deliberately NOT persisted: not every cached value is worth a durable commit point. Consequence review: (1) ceremony state (magic links, OTP codes/flows, OIDC parked requests/auth codes/consent/device flows, social PKCE sessions, passkey challenges, TOTP enroll tokens) is short-lived, one-shot, and **fails closed** on loss — a restart or a mis-routed multi-instance request costs the user one ceremony retry, with zero security consequence; (2) throttles (SEND_THROTTLE, verify-failure lockouts, per-IP limiters) degrade to ~N× budget across N instances — weakened, never bypassed, and the OTP brute-force math stays negligible (5 failures per instance vs 10⁶ codes); (3) everything with a security *durability* requirement is already shared/persistent: revocation + refresh-family poison + client-machine markers in `jwt.db`, signing keys/users/policies/clients in the tenant DBs. Single-instance-per-data-dir is the documented deployment contract (README ops notes, DESIGN.md §6 scope, INTEGRATION.md containerization note); multi-instance HA (sticky routing or a shared ceremony store) is a future *feature* if ever wanted, not a gap.
- **G-122** — `user.active` transitions now level-gated (`require_above_user`), self-reactivation blocked, activeness enforced at login/refresh boundaries (`src/user.rs:165-189,665-672`, `src/db.rs:280-282,364-378`). Still open: the write is an unconditional update (no CAS), and there is no actor-level audit of who deactivated whom (G-89).
- **G-28** — Integration tests now traverse the real `protect` hoop with real root/admin sessions and seeded policies (`tests/common.rs:139-155,404-458`). Still open: ~14 admin tests assert only `resp.is_ok()` (passes on 401/403/500 — `tests/z_integration_tests.rs:95,115,161,180-208,…`), `*_requires_auth` tests assert nothing about auth, and **no HTTP-level test asserts the 403 deny path for a valid session with an insufficient role**.
- **G-127** — CI single-threads the lib suite (`ci.yml:73-77`) and test envs pin singletons to process-lifetime runtimes; root cause remains: first-call-wins process globals (`ENCRYPTION_KEY: OnceLock` set with `let _ =` from 14 test sites; global revocation store). `tests/unit/crypto_unit.rs:3-4` documents key-validation tests *removed* because of the OnceLock.

### Worsened since filed

- **G-96** — Client lifecycle still create + soft-delete only (`src/idp.rs:376-389`; `secret_grace_until` declared but never set or checked), and the "delete + recreate" rotation workaround is now **impossible for the same identifiers**: the soft-deleted row squats the `client_id` PK and its `RedirectURI` rows are never removed (see G-143). No reactivation path exists.
- ~~**G-123** — Client delete still doesn't revoke its 90-day machine tokens (`validate_token` never consults the `OAuth2Client` row, `src/utils.rs:540-623`), and the filed mitigation ("sweep them through `/revoke`") is a silent no-op — see G-130. The only admin-side kill switch today is signing-key deletion (G-97, tenant-wide collateral) or waiting 90 days.~~ **(fixed 2026-09-07)** `oauth2client_delete` now poisons a persistent `machine_client:{client.uuid}` revocation marker (the `session_family:*`/`oidc_refresh_family:*` precedent) with a 90-day TTL matching the machine-token lifetime, and `validate_token` enforces it: every outstanding machine token of the deleted client fails as `Revoked` on its next presentation — SCIM `protect`, introspection, userinfo, all of them. The check is scoped by the mint-time `client:<id>` username convention (new `TokenPayload::machine_username`), so user sessions never pay the extra store lookup and pre-fix tokens are covered (the marker keys off `sub`, which every machine token has carried since day one). Ordering is deactivate-then-poison (a poison failure can never leave an *active* client whose tokens are rejected) and the write error is surfaced, not swallowed — the delete is idempotently retryable. `/revoke` sweep also works now (G-130). Tests: `client_delete_poisons_its_machine_tokens` (live before → marker poisoned → `TokenReject::Revoked` after → issuance closed).

---

## 🔴 New — High

### ~~G-128. Role revocation never propagates: refresh re-mints stale roles~~ (closed 2026-09-07)
`src/db.rs:380-381`

`Tenant::refresh_jwt` copied `JwtData` from the presented token and refreshed only `username` from the DB; `roles` were re-minted verbatim, and the policy engine reads roles exclusively from the token. Removing a role or demoting an admin had **no effect** on any session that kept calling `/api/v1/auth/refresh` — and the internal chain has no absolute cap (G-151), so a demoted admin retained admin API access indefinitely.

**(fixed 2026-09-07)** `refresh_jwt` now reloads roles from the DB on every rotation, exactly as the login path (`authenticate_jwt`) does — a revoked role stops propagating within one token lifetime (15 min), the same contract deactivation already had (`src/db.rs:296-301`), and fresh grants are picked up on the next refresh (`src/db.rs:401-412`). `mfa` deliberately stays as-minted: it is authentication *history* bound to `auth_time` (feeds `amr`/`acr`), not the current credential inventory — re-pulling it would misreport how the session actually authenticated. Scope check: `refresh_jwt` was the only stale re-mint point — OIDC tokens carry no roles (`OidcRefreshTokenData`/`OidcAccessTokenData` hold scope/mfa/family only), and every factor verify (incl. TOTP step-up) mints through `authenticate_jwt`, which always reloaded roles. Regression test `refresh_reloads_roles_so_revocation_propagates`: mint with `admin` → revoke `admin` + grant `guest` → rotate → assert the new token lost `admin` and gained `guest`. Verified: `cargo test --lib --test unit_tests -- --test-threads=1` → 290 + 90 passed, integration 31/31, clippy `-D warnings` clean. Residual: already-minted tokens keep their stale roles until natural expiry (stateless verification by design); the unbounded internal refresh chain remained tracked as G-151 (closed 2026-09-08), and sessions-invisibility as G-106.

### ~~G-129. Privilege escalation: `policy_create` can bind root-only resources to any sub-level role~~ (closed 2026-09-07)
`src/policy.rs:295-297`, `src/role.rs:147-152,241-252`, `src/user.rs:368-385`, `src/admin.rs:93-106,218-242`

The level gate checked only the target *role's level*, never the *resource* being attached. An admin (80) could: create a custom role at level 79 → attach the root-only `/api/v1/admin/tenant/delete|create|list` policies to it → grant themselves that role → delete/create tenants (handlers act globally on `Storage` with no additional root check), contradicting DESIGN.md:33,37 ("a policy can widen *which* endpoints are callable, never *what power* they confer"; "tenant/* bound to root only").

**(fixed 2026-09-07)** Fixed on two axes, because during implementation a binder-only check proved trivially bypassable: the engine's resolver branch (`FromQuery`/`FromHeader` targets, and `source=Nothing` shapes) never matched `resource` against the request path at all — a policy with an innocent-looking resource applied to **every** path in the domain, so an admin could grant a sub-level role the tenant surface without ever naming it.
- **Binder (resource-power gate):** `Tenant::require_root_power_for` (`src/policy.rs`) refuses to bind a root-powered resource unless the caller's effective level reaches the apex — `Bootstrap` or a `root` (100) session. The check is template-aware: `{param}` segments cover literals, so evasions like `tenant/{op}` or `{surface}/delete` are caught, and leading-slash variants are normalized. Wired into both `policy_create` (R5) and `policy_delete` (R6), for allow and deny rows alike. `ROOT_LEVEL` is tied to `BUILTIN_ROLES[0].1` (`src/role.rs`) so catalog and gate cannot drift. Root may still delegate the lifecycle surface downward (e.g. `tenant/list` to a platform-auditor role); root's *own* policy set remains unalterable via the API (`require_below` unchanged).
- **Engine (path constraint):** `can_access` now requires the resource template to match the request path in **every** branch (`resource_matches_path`: equal segment count, `{param}` covers any value) — the documented self-scoped shapes (`User`+`FromQuery ?owner=…` etc.) keep working but are finally confined to their resource; the exact-match branch (Nothing+Nothing) is untouched, preserving the SCIM canonicalization contract ("matches exactly, no wildcards", `src/verify.rs:139-143`). All seeded/standard policies are Nothing+Nothing, so no shipped policy changes behavior.
- **Tests:** the HTTP-level expectation that codified the vuln (`policy_create_denies_own_and_superior_roles` asserting OK for admin binding `tenant/delete`→`user`) is inverted to FORBIDDEN with the downward-write case moved to a non-root resource; new lib gates `policy_create_root_resource_requires_root_power` (all three tenant paths, two template evasions, slash-less form, deny rows, resolver-shape smuggling, root/Bootstrap delegation, non-root resources unaffected) and `policy_delete_root_resource_requires_root_power`; new engine tests `test_query_target_policy_does_not_leak_to_other_paths`, `test_nothing_source_with_query_target_is_path_constrained`, `test_param_template_stays_within_its_shape`. Verified: 292 lib + 93 unit + 31 integration passed (`--test-threads=1`), clippy `-D warnings` clean.
- Residual (tracked elsewhere): `/api/v1/admin/metrics` is process-global yet bound to `admin` in the standard set — cross-tenant telemetry disclosure, filed as a separate Low finding; domain registration remains an operator trust decision (G-148 closed by design 2026-09-08 — see DESIGN.md §5 for the policy re-pointing mitigation).

### ~~G-130. Machine tokens are invisible to `/revoke` and `/introspect`~~ (closed 2026-09-07)
`src/oidc.rs:3372` (revoke), `:3640-3648` (introspect), `src/db.rs:140-150`, `src/oidc.rs:510-516`

Both endpoints bound the presented token to the calling client via `data.get("client_id")` — but `JwtData` has **no `client_id` field**; for `client_credentials` tokens the client id lives only in `aud`. The lookup yielded `""` ≠ `client.id`, so `/revoke` returned RFC-7009 200 **without recording any revocation**, and `/introspect` reported `active: false` for a live token — killing the code's own documented contract ("expiry is a backstop and `/revoke` is the off switch") and the only standards-based remedy for a leaked 90-day SCIM principal.

**(fixed 2026-09-07)** Both handlers now fall back to the machine-token binding when `data.client_id` is absent: `aud == client.id` corroborated by the mint-time `client:<id>` username convention — both inside the signed envelope, so a presenter cannot forge them, and the fallback binds *only* the owning client (a foreign client's attempt stays a silent no-op per RFC 7009 §2.1 / 7662 §2.1). Shared predicate `machine_token_belongs_to` (`src/oidc.rs`); works for tokens minted before the fix (no format change). Introspection of an own machine token now reports `active: true` with `client_id` = the caller's id and `sub` = the service uuid; revocation records the token in the persistent `InvalidJwt` store, after which `validate_token` rejects it everywhere (SCIM protect included). Deliberate boundary kept: public clients remain refused on both endpoints (existing posture). Tests: `revoke_machine_token_by_its_own_client_kills_it`, `revoke_machine_token_by_foreign_client_is_a_noop`, `introspect_machine_token_binds_to_its_client` — all minting through the real `/token` endpoint. Together with the G-123 marker (client delete) and G-97 (key retirement), the machine-token kill chain is complete: per-token off switch (`/revoke`), per-client off switch (delete), and rotation without collateral (retire).

### ~~G-131. Bootstrap lockout: seeded/admin-created users can never obtain a first credential~~ (closed 2026-09-07)
`src/email.rs:61-85`, `src/otp.rs:57-77`, `src/seed.rs:159-167`, `src/user.rs:451-467`, `src/router.rs:286-300`

Seeded `admin`/`demo`, `admin/user/create`, and `bootstrap_tenant`'s first admin were all created credential-less. Strict signup fails on the pre-existing `#[unique] name`; strict signin requires the credential to already be attached; there was **no admin endpoint to attach a first credential**; SCIM needs a `scim` client only an admin session can register. Chicken-and-egg: pre-creating a username locked that user out; only DB surgery recovered.

**(fixed 2026-09-07)** Three vouching paths, all landing the credential VERIFIED (admin/deployment vouches — the same trust model SCIM email provisioning uses; no ownership ceremony, so no takeover window):
- **Seed:** `UserDTO.email` (optional, `src/user.rs`) attaches at seed time via `email_create_verified` — idempotent on every boot, which makes "add the email, restart" the repair path for an already-booted credential-less admin. Both seed files now ship the admin with a placeholder `email` and the stale SECURITY note (claiming the removed attach-on-first-verify behavior) is rewritten.
- **Runtime tenants:** `NewTenant.admin_email` → `bootstrap_tenant` attaches the first admin's vouched credential (`src/admin.rs`, `src/seed.rs`) — a runtime-created tenant is operable immediately instead of admin-locked.
- **Repair/general:** new `POST /api/v1/admin/user/attach_email` (`src/email.rs::attach`) — H3 level-gated like every credential mutation (`require_above_user`: admin cannot vouch onto root), lowercases at the wire boundary, idempotent re-attach for the same owner, foreign-owned address → 400, unknown user → 400. Route + `STANDARD_ADMIN_POLICIES` + both seed files + `tests/common.rs` mirror row.
- Email bootstraps everything else: after the first magic-link signin, mobile/social/passkey/TOTP attach through the existing (now sudo-gated, G-132) self-service flows.
- Docs: INTEGRATION.md intro + Phase 1/2 rewritten (vouched-inbox quickstart, strict-signin explanation, attach_email as the later-admin path); `tests/compliant/README.md` enabler #2 marked landed.
- Tests: `attach_email_vouches_a_first_credential` (HTTP-level: vouch→verified+lowercase, idempotent, foreign-owned 400, unknown user 400, H3 403), `seed_user_email_attaches_verified_credential` (idempotent re-seed), `bootstrap_tenant_provisions_catalog_policies_and_admin` extended (admin_email → verified credential, signin-not-signup). Residual: no admin-console UI for attach yet (API-only; G-163 territory).

### ~~G-132. No step-up/re-auth for credential changes → persistent takeover from a hijacked session~~ (closed 2026-09-07)
`src/email.rs:846-862`, `src/otp.rs:727-743`, `src/social.rs:720-729`, `src/passkey.rs:475-477,669-692`, `src/verify.rs:194-205`

Adding a login credential (email/mobile/social/passkey/TOTP) or deactivating **all** passkeys required only a bearer session — never a fresh factor. The `session` hoop deliberately skips the policy engine, so a tenant could not even configure an MFA requirement for these endpoints. A stolen session could attach attacker-owned credentials and yield persistent access surviving revocation of the stolen token.

**(fixed 2026-09-07)** Sudo mode for credential mutations: `verify::session_is_fresh` requires the session to have been **authenticated** — not merely refreshed — within `SUDO_WINDOW_SEC` (15 min, deliberately equal to the session TTL: a chain that has been through `refresh_jwt` is out of the window, because rotation preserves the original `auth_time`, so a stolen-and-rotated session can never re-enter). Missing `auth_time` fails closed. Gated: `email/add`, `otp/add`, `social/link`, `totp/enroll`, passkey **registration** (the session branch of `passkey/request`), and `passkey/remove` — refusals are 403 + machine-readable `X-Reauth-Required: true` (mirrors the `X-MFA-Required` precedent), each factor keeping its own wire body. Chosen over routing these through the policy engine: the engine's `mfa` gate hardcodes TOTP (G-138 — no TOTP UI exists, so it would lock everyone out) and protect-routing needs per-tenant policy rows; sudo mode works for every tenant with zero config. The login ceremonies themselves (`request`/`verify`, TOTP step-up completion) stay ungated — gating them would be the circularity the session hoop exists to avoid. Tests: `sudo_gate_accepts_only_fresh_auth_time` (window edge, stale, missing, skew) + six handler regressions (`add_email_refuses_stale_session`, `add_mobile_…`, `enroll_…`, `link_…`, `passkey_register_…`, `passkey_remove_…`), each with a fresh-session positive control. Residuals: `user/delete/self` stays protect-routed (tenants *can* bind `mfa=true` to it — a policy row, not a hoop gap); a session stolen inside its first 15 minutes still has a bounded sudo window (G-139 removes the XSS exfiltration path).

### ~~G-133. Default magic links 404: seeded `verify_url` points at a nonexistent route~~ (closed 2026-09-07)
`seed.example.toml:153`, `seed.toml:138`, `docs/INTEGRATION.md:23,40`, `src/router.rs:124-142`

Both seed examples and the integration guide set `verify_url = "http://localhost/api/v1/auth/email/landing"` — no such route exists; the static catch-all 404s. Out of the box, the primary factor's emailed link was dead, and no test covered the landing-route contract.

**(fixed 2026-09-07)** `verify_url` now points at the hosted `/login` SPA in both seed files (with a comment explaining the contract), and INTEGRATION.md Phase 1/2 match. The contract is pinned by `seed_toml_bootstraps_builtin_roles`, which parses the seed through the production config path (local `seed.toml` when present, committed example otherwise — so both files are validated across environments) and asserts `Url::parse(verify_url).path() == "/login"`. The link builder appends exactly the params the SPA consumes — `token`/`username`/`email` plus the optional `client_id`/`state`/`redirect_uri` return context (`src/email.rs::build_magic_link` ↔ `frontend/src/login/App.tsx:33-44`), and the SPA's verify URL itself comes from discovery with the well-known path as fallback (G-25), so a tenant that re-points `verify_url` at its own landing page keeps working.

### ~~G-134. `.env.example` is dead config; README directs live secrets into it~~ (closed 2026-09-07)
`.env.example:4-41`, `src/main.rs:89`, `src/server.rs:327,334`, `README.md:25,33,70`

The server reads only `JANUX_CONFIG_FILE`, `RUN_ENV`, and `JANUX__*` overrides; there is no dotenv dependency and zero `env::var` reads for any listed key. README nonetheless instructed operators to put provider credentials in `.env` — real secrets landed in a file nothing consumes. Roughly half of `.env.example` was config for an unrelated LLM/RAG product.

**(fixed 2026-09-07)** `.env.example` rewritten to document exactly the three real mechanisms — `JANUX_CONFIG_FILE`, `RUN_ENV` (selects the optional `config/{RUN_ENV}.toml` overlay), and `JANUX__<FIELD>` overrides (config-crate `Environment` source, prefix `JANUX`, separator `__`, highest precedence) — with an explicit "NOT environment variables" section pointing provider credentials at their real home (`[seed.resend]`, `[seed.alisms]`, `[[seed.providers]]` in seed.toml) and a note that nothing loads `.env` automatically. The unrelated LLM/RAG block is gone. README: quickstart no longer copies `.env` (and now tells operators to set the admin's seed `email`, G-131), the "no providers configured" line points at `seed.toml` only, and the config table row describes the file accurately. **Residual (operator-side, cannot be fixed from the repo):** the local gitignored `.env` still holds what appear to be live Aliyun/Resend/GitHub secrets for keys the server never reads — rotate and delete them.

### ~~G-135. Release pipelines publish unverified artifacts~~ (closed 2026-09-08)
`.github/workflows/release-binaries.yml:85-130`, `release-docker.yml:76-79,131-134,200`

Binaries: zero tests/lint before `cargo build --release`, no `needs:` on CI, no sha256 checksums, no signing, no post-build smoke check (not even `--version`); Windows amd64/arm64 published though no test ever runs on Windows; macOS artifacts attached manually from a dev machine. Docker: `provenance: false` + `sbom: false`, the built image is never run (no health probe), only verification is `imagetools inspect | head -20`. Tag pushes release regardless of CI state on that commit.

**(fixed 2026-09-08)** Binaries: a `verify` job now runs the CI test command (`cargo test --lib --test unit_tests -- --test-threads=1`, frontend built for the rust-embed step) on the tagged commit, and `binaries` has `needs: [verify]` — a tag no longer publishes regardless of test state. Every built artifact is smoke-checked with `janux --version` before packaging (the one documented exception: cross-compiled Windows arm64 cannot execute on its amd64 build host), and each asset ships with a `.sha256` checksum (`sha256sum`/`Get-FileHash`) that the release body tells users to verify. Docker: a new `docker-smoke` job pulls the amd64 digest, **runs** it with a minimal generated config, and probes `/api/v1/healthy` for up to 60 s (dumping container logs on failure); `docker-publish` now `needs` it, so a broken image never receives the user-visible `latest`/version/SHA tags (per-arch pushes are by-digest and untagged until smoke passes). Residuals (deliberate): `provenance`/`sbom` stay disabled because ACR Personal Edition rejects buildx attestation manifests (constraint now documented in the workflow header); no artifact signing (sigstore/minisign) — checksums are unsigned; macOS remains a manual local build (paid-runner constraint, documented); Windows arm64 remains run-unverified.

### ~~G-136. Compliance suite is CI-invisible and structurally blocked~~ (closed 2026-09-08)
`tests/compliant/` (49 tests), `.github/workflows/ci.yml` (no Python step), `tests/compliant/conftest.py:14-19,82-89`, `tests_op/test_magic_link.py:7-14`, `tests_op/test_jwks.py:19-21`, `tests_op/test_discovery.py:102-110`

`tests/compliant` appeared in no workflow, and the code-flow/PKCE/refresh/revocation tracks skipped or strict-xfailed behind four unimplemented server enablers.

**(fixed 2026-09-08)** All four enablers landed and the suite runs **60/60 green, zero skips/xfails, ~6 s**:
- **Rate-limit knob:** top-level `disable_rate_limits` (`JanuxConfig`, default false) widens every per-IP quota (auth 6/min, OIDC public 12/min, admin 12/min, SCIM 60/min) via `router::quota` — hoop order and the 429 machinery stay intact; threaded through `api`/`public_routes`/`scim::router`/`api_with_doc`. Loudly documented TEST-CONFIG-ONLY. The harness config sets it.
- **Seed user emails:** landed via G-131 (`UserDTO.email`); the harness config now vouches `admin@{domain}`, and the `admin` fixture's graceful skip became a hard failure (a blocked login is now a regression, not a known gap).
- **Seed signing key:** `TenantDTO::save` creates one key per seeded domain (`seed-<domain>`, idempotent across restarts) — JWKS is populated from first boot and ceremony JWTs can be issued; `test_jwks_not_empty` flipped from strict-xfail to a real assertion.
- **Discovery:** `client_credentials` added to `grant_types_supported`; the strict-xfail flipped to a real assertion.
- **Harness bugs the blockage had hidden:** `load_jwks` built `JWKSet(keys=[...])` (jwcrypto passes the list to `add`, which only accepts JWK objects — TypeError); magic-link assertions expected `sub` to be the username (janux flattens `JwtData`: the name is top-level `username`, `sub` is the UUID); public-client registration collided with the session fixture's redirect URI (the global `RedirectURI` keyspace, G-143 — worked around with a unique callback, skip guard removed).
- **CI:** new `compliance-tests` job (needs unit-tests): builds the debug binary the harness spawns, `uv sync --frozen && uv run pytest -q`.
- Residual: the external OIDF certification driver (`oidf/`) stays manual with placeholder secrets — that is G-124, still open.

### ~~G-137. Advertised crypto/key unit coverage is illusory~~ (closed 2026-09-08)
`tests/unit/key_unit.rs:56-58,87-91,108-130`, `tests/unit/crypto_unit.rs:1-4`, `tests/README.md:53,56`

`key_unit.rs` never imported `janux` — it re-implemented `compute_at_hash` locally and asserted tautologies; `crypto_unit.rs` was a 4-line stub with zero tests; `tests/README.md` claimed both as real coverage. `src/crypto.rs` — the AES-256-GCM at-rest module everything recovery-critical depends on — had **zero direct tests**.

**(fixed 2026-09-08)** Both files rewritten against real product code, and the README table now describes what actually runs:
- `crypto_unit` (5 tests): key-validation ordering (invalid hex/lengths rejected WITHOUT consuming the first-call-wins OnceLock), AES-256-GCM round-trips (incl. empty + unicode + 4 KiB), probabilistic encryption (64 encrypts of one secret → 64 distinct ciphertexts), tampered/non-base64/empty/sub-nonce ciphertext rejection, and the `decrypt_secret_or_legacy` fallback contract (with the G-150 wrong-key caveat documented inline). Residual (inherent): wrong-KEY rejection is untestable in-process while the key is a first-call-wins global — noted in the file header.
- `key_unit` (4 tests): exercises the real `janux::jwt::compute_at_hash` — pinned spec vector (`at_hash("at_12345") == "OdClz_bNRQ96i3tyxufAsA"`, computed independently), cross-check against a reference construction, determinism/token-sensitivity, base64url-no-pad 22-char shape. As part of this, the at_hash computation was **consolidated**: three inline copies in `oidc.rs` and a private `Option`-wrapper in `jwt.rs` became the single pub fn (the RSA key lifecycle itself is covered by the lib tests in `src/key.rs`/`src/db.rs` — it needs a database, and the README now says so instead of overclaiming).
- Verified: `cargo test --test unit_tests` → 94 passed (was 90, with 15 of those previously being tautologies/stubs).

### G-138. Frontend has no TOTP UI — MFA policies are a dead end
`frontend/src/login/App.tsx:129-133`, `src/router.rs:199-213`, `src/policy.rs:188`, `frontend/src/api/sdk.gen.ts:583-619`

The login app renders only `email`/`otp`/`passkey`/`social`. The admin UI can create `mfa: true` policies, but no shipped page can enroll TOTP or complete the step-up round — the generated SDK exports `openapiTotpEnroll`/`openapiTotpVerify` and nothing imports them. Any MFA-gated resource is permanently unreachable through the built-in frontend. (Discovery's `janux_factors` never advertises totp either, `src/oidc.rs:724-786`.)

### ~~G-139. Session JWT stored in `sessionStorage`; the backend's HttpOnly-cookie option is never used~~ (closed 2026-09-07)
`frontend/src/shared/session.ts:1-13`, `src/email.rs:220-227,620-627`

Any XSS on the origin could exfiltrate a 15-minute all-factors session JWT (compounding G-132 into persistent takeover). The server already set HttpOnly/Secure/SameSite=Strict cookies and `VerifyRequest.cookie` existed in the wire contract — no frontend call site ever set it.

**(fixed 2026-09-07)** The session JWT now lives in a canonical `janux.session` **HttpOnly cookie** end to end, and JS never persists it:
- **Server:** `utils::SESSION_COOKIE` + `get_jwt` reads the Authorization header first and falls back to the canonical cookie (raw-header parse; JWTs are base64url) — one choke point, so every validation primitive (`protect`, `validate_jwt`, `validate_session`, SCIM, the OIDC SPA endpoints) is cookie-aware. `logout` and `/end_session` expire the cookie server-side (JS cannot remove an HttpOnly cookie); `refresh` re-sets the rotated token into the cookie when the old one arrived by cookie (rotation revokes the predecessor — without this the jar would keep presenting a dead token). `social/redeem` gained the `cookie` field the other verify endpoints already had.
- **Frontend:** every ceremony passes `cookie: "janux.session"` (email landing verify, OTP verify, passkey verify, social redeem); `shared/session.ts` now stores only a non-sensitive per-tab presence marker (`markSession`/`hasSession`); admin/consent/device apps authenticate purely via the browser-attached cookie (same-origin fetch), no Bearer storage; the JWT is used in-memory solely for the immediate post-login resume hop.
- **CSRF posture unchanged:** SameSite=Strict kept — cross-site requests carry no cookie at all, every mutation is POST; Bearer still wins for non-browser clients, so curl/RP/SCIM integrations are untouched.
- Tests: `get_jwt_prefers_bearer_and_falls_back_to_the_session_cookie` (precedence, case-insensitive scheme, multi-cookie header, non-canonical and empty-value ignored), `session_cookie_set_and_clear_shapes` (attribute set + expired clear shape round-tripped through a real response); frontend build/lint/unit green; OpenAPI client regenerated for `RedeemRequest.cookie`. Docs updated (DESIGN §8 logout bullet, FRONTEND session contract, INTEGRATION quickstart).
- Residual (parity, not regression): the admin UI's presence marker is per-tab, so a fresh tab still bounces to login even though the cookie is browser-wide; silent SSO on cross-site RP-initiated navigation would need SameSite=Lax + a server-side session check at `/authorize` — filed as a future improvement, not attempted here.

---

## 🟠 New — Medium

### Backend security & correctness

- ~~**G-140.** One-shot secrets accepted as URL query params and logged verbatim by the audit hoop (full `req.uri()` at info level): magic-link tokens (`src/email.rs:547-558`), SMS tokens/codes (`src/otp.rs:420-433`), TOTP codes + 15-min enrollment JWTs (GET-routed, `src/router.rs:208-213`), social IdP `code`/`state` (`src/router.rs:188-193`).~~ **(fixed 2026-09-08)** The audit hoop now logs `redacted_uri(req.uri())`: values of secret-carrying query params (`token`, `code`, `jwt`, `access_token`, `refresh_token`, `id_token`, `id_token_hint`, `logout_token`, `assertion`, `client_secret`, `secret`, `password`, `user_code` — case-insensitive) are replaced with `[redacted]`, while names and innocent params (`state`, usernames) stay so the trail remains useful. Test: `audit_uri_redacts_secret_query_values`. Residual: the deeper fix — moving these secrets out of query strings into POST bodies entirely — is a wire-contract change (the GET-routed `totp/verify` variants, magic-link landings) and stays open as a Low-tier item.
- ~~**G-141.** OIDC SPA endpoints compute the policy verdict and ignore it: `/authorize`, `/authorize/resume`, `/consent/info`, `/consent/submit` authenticate via `validate_jwt` (runs default-deny engine) but `can_access` is never consulted; `domain_bound` not set — a sibling-domain session drives another domain's authorize flow; `expect_mfa` unenforceable here (`src/oidc.rs:1102,1270,1468,1540`).~~ **(fixed 2026-09-08)** All four switched from `validate_jwt` to `validate_session` — the domain-bound, policy-free primitive the other ceremony endpoints already use. Two birds: no verdict is computed-to-be-ignored anymore, and `data.domain` must equal the request domain (issuer check already pinned `iss`, so cross-domain tokens now fail two independent locks). Honoring `can_access` directly would have been WRONG: these public ceremony routes have no policy rows, so the default-deny engine returns `can_access == false` for every session — the switch is documented at the call sites to prevent that "fix" from being reintroduced. Test: `authorize_resume_refuses_sibling_domain_session`; the existing consent/resume flow tests pin the happy path. Residual: `expect_mfa` remains unenforceable on ceremony routes by design (the documented circularity — a token denied for missing MFA must still be able to complete MFA).
- ~~**G-142.** Auth-code replay returns `invalid_grant` (one-shot consumption) but never revokes tokens previously issued from that code — RFC 6749 §4.1.2 SHOULD-half absent; `AuthGrant.code_hash` is write-only, never read back. A detected replay (canonical stolen-code indicator) leaves the attacker's tokens alive.~~ **(fixed 2026-09-08)** The refresh family now DERIVES from the grant jti (`family = jti` at exchange, instead of a fresh random UUID), making the persistent `AuthGrant` row (new `auth_grant_by_code_hash` lookup) the code→family map. On a one-shot-cache miss the exchange checks that map: a matching grant proves tokens were issued, so the replay response poisons `oidc_refresh_family:{jti}` (killing the whole refresh chain, including reuse-detection semantics), marks the grant revoked (withdrawing the consent it carries), and logs a warning with the grant/client identity. Test: `auth_code_replay_revokes_issued_tokens` (first exchange yields a refresh token → replay 400s → family poisoned → the issued refresh token no longer rotates → grant row revoked). Residuals: the ≤60-min access token expires on its own (no jti-keyed revocation index exists); device-flow codes keep random families (their replay window is the polling loop — separate contract); detection is only as durable as the grant row (it is — DB-backed).
- **G-143.** `RedirectURI` PK is the URI string itself, **global across clients**: two clients can never share a callback; soft-deleted clients permanently squat URIs (blocking G-96 rotation); non-transactional client create leaves half-registered clients on conflict; with DCR enabled an anonymous registrant can squat a victim RP's callback (`src/idp.rs:72-88,279-298,376-389`; DCR rollback covers only the metadata save, `src/oidc_ext.rs:319-326`).
- ~~**G-147.** `admin/provider/list` serializes `client_secret`: `SocialProvider` derives `Serialize` including the field and `all_providers` renders the raw model — legacy (pre-encryption) rows are readable **plaintext**, newer rows ciphertext; secrets should be write-only.~~ **(fixed 2026-09-08)** The endpoint now maps to a `ProviderEntry` wire DTO (`id`, `client_id`, `issuer_url`, `scopes`) — `client_secret` is write-only everywhere on the API surface; the model keeps `Serialize` for internal use only. The admin console's `ProviderRow` interface already excluded the secret, so no frontend change was needed; OpenAPI client regenerated. Test: `provider_list_never_serializes_client_secret` (asserts neither the field name nor the plaintext value appears, while non-secret fields do).
- ~~**G-148.** No domain-ownership validation on `add_domain`: only global uniqueness + tenant match; no DNS/HTTP challenge, no format validation. Any tenant admin can squat an unregistered hostname, and since tenant/issuer resolution is a pure Host lookup, any traffic arriving under that host is served — ceremonies and token issuance included — by the claiming tenant (`src/admin.rs:177-203`, `src/utils.rs:400-430`).~~ **(closed 2026-09-08 — by design, owner decision)** Ownership attestation is not Janux's responsibility: DNS-01/HTTP-01/TLS-ALPN challenges presume public DNS and TLS infrastructure that the supported deployment modes (intranet hostnames, localhost dev, proxy-fronted and plain-HTTP origins) do not have, and the only party who can vouch for a hostname is the operator controlling its routing — the same line Keycloak/Authentik-class products draw. The mechanisms for wary deployments already exist and are now documented in DESIGN.md §5: `domain/add|delete` are policy rows that can be re-pointed from `admin` to a higher-trust role (default-deny then removes them for everyone else), tenant creation stays root-only, and uniqueness prevents cross-tenant collision. Residual note for shared-hosting operators: whoever can call `domain/add` can claim any hostname that routes to the server — treat that role assignment as the trust boundary it is.
- **G-149.** `trust_forwarded_headers = true` ships as the example default with no boot validation or warning (`base.example.toml:14`, `src/main.rs:97-120`). If directly reachable in this mode: forged `X-Forwarded-Host` picks tenant/issuer context (first candidate wins), forged XFF (rightmost entry trusted) defeats every per-IP limiter and the audit IP trail (`src/utils.rs:292-315,874-885`). **(partially fixed 2026-09-08)** The example now ships `false` (safe standalone default) with a comment explaining exactly when `true` is safe, and boot logs a loud `tracing::warn!` whenever header trust is enabled; README + INTEGRATION.md Phase 1 updated to match. Still open: no structural validation that a proxy is actually in front (e.g. an allowed-proxy-IP list constraining which sources may speak X-Forwarded-*).
- **G-150.** Encryption-key handling: the well-known example key `12345678…` is accepted silently (boot checks presence only, `src/main.rs:104-109`); `decrypt_secret_or_legacy` returns the stored value on **any** decryption failure — after a key change, ciphertext is fed to providers as if it were the secret (opaque send failures) and legacy plaintext rows never force-migrate (`src/crypto.rs:74-76`); single `OnceLock` key, no key-id/version in ciphertext, rotation = manual decrypt/re-encrypt everything, no tooling. **(partially fixed 2026-09-08)** Boot now warns loudly when `encryption_key` equals the well-known example value ("secrets at rest are effectively plaintext — `openssl rand -hex 32`"). Still open: key versioning/rotation tooling and the silent legacy-ciphertext fallback semantics.
- ~~**G-151.** Internal session refresh chains have no absolute lifetime: `auth_time` is carried forward unbounded; an internal session can rotate every 15 min forever. The OIDC path caps families at 30 days; the internal path — which gates the admin API — has no equivalent. Compounds G-106/G-128.~~ **(fixed 2026-09-08)** `refresh_jwt` now enforces `INTERNAL_SESSION_MAX_AGE_SEC` (30 days, matching the OIDC family cap): a chain whose original `auth_time` is older than the cap must re-authenticate, and a missing `auth_time` fails closed. Test: `refresh_rejects_chain_past_absolute_cap` (aged chain refused with "absolute lifetime", in-cap aged chain still rotates). Combined with G-128 (roles reloaded per rotation), a stolen chain now has both a hard expiry and live authorization state.
- ~~**G-152.** `SEND_THROTTLE` keys (`email:<addr>` / `mobile:<digits>`) have no tenant/domain component: two tenants share one 3/min per-recipient budget, so an attacker with any account on tenant A can exhaust magic-link/SMS delivery for a victim on tenant B.~~ **(fixed 2026-09-08)** All four call sites (email request/add, OTP request/add) now key the throttle `"{domain}|email:…"` / `"{domain}|mobile:…"`, matching the domain-scoped verify-failure gate; the `send_throttle_allows` doc makes tenant-scoping a stated contract for future callers. Test: `send_throttle_is_scoped_per_domain` (3 sends + 4th throttled on domain A; the same recipient keeps its own budget on sibling domain B).
- **G-153.** Network sends while holding the tenant write guard: login flows were fixed (H6) to drop the guard before the provider hop, but `email/add` sends under it (`src/email.rs:822`, guard from `:886`), `otp/add` likewise (`src/otp.rs:800`/`:768`), and social does IdP discovery + token exchange under it (`src/social.rs:642-652,847-879`). A slow/hung peer stalls every request for that tenant up to the 15 s timeout.
- ~~**G-154.** `role_delete` removes only the `Role` row: its `Policy` rows stay in the DB **and** the in-memory `PolicyCache` (no eviction, unlike `policy_delete`), `UserRole` rows orphan; outstanding tokens naming the role keep passing evaluation; re-creating a role with the same name silently resurrects every stale policy without passing `policy_create`'s gate.~~ **(fixed 2026-09-08)** `role_delete` now cascades the role's whole footprint: `Policy` rows deleted from the DB **and** the role's entry evicted from every domain's `PolicyCache`, plus all `UserRole` memberships removed — before the `Role` row itself goes. Tokens minted before the delete keep naming the role until expiry, but the name now resolves to no level, no policies, and no grant; a same-named re-creation starts clean instead of resurrecting stale policies around the gates. Test: `role_delete_cascades_policies_and_memberships` (cache eviction, DB rows gone on fresh load, membership unresolvable, clean re-creation).

### SCIM

- **G-144.** Unfiltered `GET /scim/v2/Users` calls `User::all()` (no LIMIT) and paginates in memory — every list materializes the whole tenant user table regardless of `count`; the admin equivalent is properly DB-paginated (`src/scim.rs:388-408`, `src/user.rs:279-298`). The 60/min limiter slows but does not bound this.
- **G-145.** `userName` handling violates RFC 7644 §5/§7.8 case-insensitivity: the filter parser lowercases the *entire filter including the quoted value*, then lookup is exact-match case-sensitive `eq` — `filter=userName eq "Admin"` searches for `admin` and misses a stored `Admin`; `#[unique] name` is case-sensitive too, so `Admin` and `admin` coexist (`src/scim.rs:342-361`, `src/user.rs:88-89,146-153`).
- **G-146.** PATCH: `remove` op rejected with 400 despite `ServiceProviderConfig` advertising `patch.supported: true`; worse, `add` on multi-valued `emails` deletes every address not in the payload (replace semantics) — a compliant IdP sending an incremental `add` silently wipes the user's other emails (`src/scim.rs:275,644-653,716-738`).

### Frontend

- **G-155.** MFA step-up signal unhandled: 403 + `X-MFA-Required` carries no body (`src/verify.rs:164-171`); admin `isUnauthorized` checks only 401 and `problemText` falls back to "Request failed (403)" — no step-up path anywhere (`frontend/src/admin/api.ts:16-23,61-80`). Since G-132 there is a second machine-readable 403 signal — `X-Reauth-Required` on the sudo-gated credential mutations — equally unhandled; the frontend has no UI for those endpoints yet (G-162), so both land naturally with it.
- **G-156.** No confirmation dialogs for any destructive admin action — user/role/policy/domain/client/key/**tenant** delete all fire directly from `onClick`; grep `confirm` across `frontend/src/**/*.tsx`: zero matches (`frontend/src/admin/App.tsx:144-152,271-279,396-406,519-533,627-635,864-872,959-967`).
- **G-157.** No token refresh in the frontend: session TTL is 15 min, `auth/refresh` exists and is exported by the SDK but never called; on 401 the admin app clears the session and hard-redirects — operators lose work mid-edit every 15 minutes (`frontend/src/admin/api.ts:11-14`).
- **G-161.** TypeScript `strict` is **off** (`frontend/tsconfig.app.json` — the stock Vite template enables it); implicit `any` across all app code; one unit-test file total (`src/login/api.test.ts`, 7 cases); no jsdom/@testing-library deps.
- **G-162.** No self-service credential-management UI despite full backend support (`email/add`, `otp/add`, `passkey/remove`, session-gated passkey *registration*, `social/link`, `user/activate/self`, `user/delete/self`): no account/settings page in any of the four apps, and the login app actively refuses the passkey-registration branch ("sign in another way first", `frontend/src/login/webauthn.ts:44-46`).
- **G-163.** Admin console surface gaps: policy UI hardcodes `allowed: true` and has no Allowed column — **deny policies can neither be created nor distinguished** (`frontend/src/admin/App.tsx:386,455-482` vs `src/policy.rs:120-122`); Method select offers 5 of 9 variants; no UI for TOTP admin, `oauth2client/meta`, OIDC/DCR config, `admin/metrics`, per-user credential removal, or a user's roles — all exist server-side and in the SDK.

### Testing / CI / docs

- ~~**G-158.** The "Playwright-driven e2e" tier has no browser anywhere: no browser-automation crate in Cargo.toml; `fixtures.rs:18` is a self-declared stub; `get_bearer_token` posts `{user, password}` to a **passwordless** server (always `None`, so dependent tests like `test_user_roles_lookup_works` assert nothing); selectors point at nonexistent `/signin.html`/`signup.html` and `input[name=password]`; 9× `is_ok()`-only assertions in `oidc_flow.rs`; structure tests `#[ignore]`d — while CI installs Chromium no test drives (`ci.yml:163-164`, `build.rs:14-39`, README:81,90).~~ **(fixed 2026-09-08)** The tier is now honest HTTP-level e2e and actually asserts: the shared server is provisioned via `TestEnv::new_with_auth` and exposes `shared_admin_token()`, so `test_user_roles_lookup_works` runs a REAL root session through protect → policy → handler (200 + `root` in the role list — it also caught that the route takes `?user=`, not `?name=`); the password fixture, `TestBrowser`, and the password-era selectors are deleted; every `is_ok()` probe became a pinned contract (discovery: provisioned doc + issuer + six endpoint fields; userinfo 401; token 401 `invalid_client`; introspect 400; revoke 400/401 per RFC 7009; JWKS 200 + **non-empty** RSA keys — which the new seed key makes true; passkey request/verify 400-not-500; `/login` + `/admin` SPA 200 at their real paths); the two `#[ignore]`d "form structure" tests were deleted (they asserted password-era markup the SPA never had) and `passkey_verify_requires_body` un-ignored; the `/authorize` error probes pin the 302→`/error` redirect contract WITHOUT following it (the followed 404 — the missing `/error` page — stays tracked as a Low finding). Playwright claims removed everywhere: `build.rs` rewritten (the can-never-match glob probe is gone), CI's Chromium install step deleted, `just e2e-setup`/`e2e-headed` removed, README lines corrected. Result: 33 passed, 0 ignored. Residual: browser-driven UI coverage still does not exist — filed as G-165.
- ~~**G-159.** `just unit` = `cargo test --test unit_tests` and `just test` never run the **lib suite** (~250 tests, the bulk of coverage: 57 in oidc.rs, 44 in utils.rs, 34 in db.rs, …) that CI runs via `cargo test --lib --test unit_tests` — including the seed-shape pin the README advertises (`justfile:25-27,49` vs `ci.yml:77`, `src/seed.rs:184`).~~ **(fixed 2026-09-08)** `just unit` now runs the exact CI command (`cargo test --lib --test unit_tests -- --test-threads=1`), so `just test` (unit → integration → e2e) covers all three Rust tiers single-threaded; added `just compliant` (conformance suite via uv) and `just backup` targets; the stale `e2e-setup`/`e2e-headed` targets are gone (G-158).
- ~~**G-160.** Dangling tracker references after the KNOWN_ISSUES.md/gaps.md deletion: `README.md:7,97`, `docs/README.md:6`, `docs/DESIGN.md:3` link the deleted file; ~26 orphaned `G-*` citations across README/DESIGN/INTEGRATION/seed.example.toml/src/tests/frontend now resolve to nothing.~~ **(fixed 2026-09-08)** The three file links (root README status line, docs/README.md index, docs/DESIGN.md intro) now point at `gaps.md`; the README Documentation section was already repointed during the G-131 batch. The ~26 in-code `G-*` citations below G-87 refer to the *historical* tracker (all closed before this review) — gaps.md's header now carries an explanatory note: those IDs are fix-provenance markers, the deleted register is recoverable from git history, and this file is the live registry for G-87 onward. Citations G-87+ all resolve to entries in this file.
- ~~**G-164.** CI has no `cargo audit`/`cargo deny` (no deny.toml/audit.toml in repo), no coverage tooling, no miri/sanitizers, no frontend `lint`/`test` gate (only `npm ci && npm run build`), no Docker build check, no non-ubuntu matrix (`ci.yml`: 4 jobs).~~ **(fixed 2026-09-08)** CI grew from 4 jobs to 7: `security-audit` (cargo-audit against the committed Cargo.lock + RustSec DB — an advisory fails the build until fixed or explicitly ignored with justification), frontend `lint` + `test` steps in the unit job (the vitest suite and eslint gate had never run in CI), `docker-build` (buildx, linux/amd64, no push, GHA layer cache — the Dockerfile had never been exercised outside releases), and `compliance-tests` (the Python conformance suite, G-136). Residuals (deliberate, cost/benefit): no coverage tooling (tarpaulin/llvm-cov), no miri/sanitizers, no non-ubuntu test matrix (release smoke checks now run the Windows amd64 artifact; the arm64 cross-build stays run-unverified — G-135 residual).
- **G-165.** No browser-driven UI test coverage anywhere: the four SPA apps (login/admin/consent/device) are exercised only through their wire contracts (HTTP e2e + the generated-client type surface). The deleted Playwright pretense (G-158) leaves the real gap visible: no test clicks through a magic-link login, the consent decision, the device-code approval, or the admin console flows. Filed 2026-09-08 as the honest successor to the removed claims; Playwright + the existing `frontend` npm toolchain is the natural stack.
- **G-166.** Audit trail v2 residuals (successor to G-89, filed 2026-09-08): (1) full old→new **diff capture** — today handlers record cheap detail facts (`active=false`, `level=79`), not before/after snapshots of the mutated resource; (2) **read-route audit policy** — sensitive reads (`key/list`, `user/list`, provider lists) are unaudited; whether reads belong in the trail is a policy choice needing a retention/noise decision; (3) **tamper-evident/external sink** — lines go to the process tracing sink only; hash-chaining or shipping to an append-only external store would survive host compromise; (4) **OIDC public mutations lack the hoop** — `revoke`, `register` (DCR), `end_session`, `consent`, device approve are not audit-wrapped (the token/authorize high-volume routes deliberately stay unwrapped); (5) social `verify` (legacy compatibility route) records no target.

---

## 🟡 New — Low (condensed)

### OIDC surface
- Post-validation `/authorize` errors (`invalid_scope`, all PKCE errors) redirect to a nonexistent `/error` page → bare 404 instead of an RP callback with `error`/`state` (RFC 6749 §4.1.2.1) (`src/oidc.rs:1095-1097,1031-1073`).
- `/token` **success** responses omit `Cache-Control: no-store`/`Pragma: no-cache` (RFC 6749 §5.1 MUST; error paths set them); 401 `invalid_client` omits `WWW-Authenticate` (`src/oidc.rs:532-543,2075-2078,2653-2661,2980-2988`).
- `/userinfo` is GET-only; OIDC Core §5.3.1 requires GET **and** POST — one-line routing gap (`src/router.rs:434-437`).
- Query-bearing registered redirect URIs corrupted: unconditional `"?code=…"` append produces `…?x=1?code=…`; `end_session` already handles this correctly (`src/oidc.rs:297-313,54-68` vs `src/oidc_ext.rs:722-727`).
- RFC 8252 §7.1 loopback port flexibility not honored — exact-string matching fails native apps on ephemeral ports, though DCR explicitly supports the loopback pattern (`src/oidc.rs:995,2376`, `src/oidc_ext.rs:56-70`).
- PKCE not required for confidential clients (OAuth 2.1 / RFC 9700 §2.1.1 mandates it for all code-flow clients) (`src/oidc.rs:1064-1073,2471`).
- Machine tokens accepted as user "sessions" on ceremony endpoints (no `typ` marker): device-login approve, `/authorize`, `/consent` — minting fails closed but side effects (AuthGrant rows, approved devices, consumed states) land first (`src/oidc.rs:4019-4033,4156-4198`).
- Back-channel logout delivery follows up to 10 redirects with no private-range filtering — bounded blind SSRF from a registered https URI; anonymous-reachable when DCR is on (`src/oidc_ext.rs:541-560`).
- Admin client creation applies none of the DCR validations (no scheme/fragment/loopback rules, no vocabulary checks on grant/response types or auth method, no secret strength) — a typo'd `token_endpoint_auth_method` bricks the client at `/token` (`src/idp.rs:542-585`).
- Discovery inconsistencies: no `scopes_supported` (fixed `KNOWN_SCOPES` vocabulary learnable only by trial); `grant_types_supported` omits `client_credentials` (implemented, and pinned strict-xfail in the conformance suite); `claims_supported` advertises `name/given_name/family_name/picture` that `/userinfo` hardcodes to `None`; no `userinfo_signing_alg_values_supported` (`src/oidc.rs:789-834,3226-3235`).
- JWKS publishes every key of the tenant across all domains (widens the accepted `kid` set, leaks sibling-domain key inventory); no cache headers; `alg` absent (`src/key.rs:357-401`).
- HTTP `Basic` scheme matched case-sensitively (RFC 7235 says case-insensitive; the Bearer equivalent was fixed as G-117) — lowercase `basic` silently falls through to the body-secret path (`src/oidc.rs:2150-2171`, `src/oidc_ext.rs:358`).
- An expired `id_token_hint` **alone** triggers full back-channel logout fan-out for that user across all RPs, repeatedly — anyone holding a historical ID token (logs/backups) can force-logout a user; only the 12/min/IP limiter bounds it (`src/oidc_ext.rs:599-653,713-717`).
- Logout never disturbs outstanding refresh-token families: an RP that missed the best-effort back-channel notification (G-126) can keep rotating and minting fresh tokens for the "logged-out" user up to the 30-day family lifetime (`src/oidc_ext.rs:695-711`, `src/verify.rs:69`).
- Public clients (`token_endpoint_auth_method: none`) are refused by `/revoke` and `/introspect` entirely — SPAs have no RFC 7009 path; deliberate anti-abuse posture, but the middle ground (client_id-scoped revocation) is unimplemented (`src/oidc.rs:3332-3344,3543-3555`).
- Duplicate `/token` (and `/revoke`, `/introspect`, `/device_authorization`) form parameters resolve last-wins instead of being rejected (RFC 6749 §2.3.1/§3.2.1) (`src/oidc.rs:1751-1765,3264-3273,3477-3486,3742-3750`).
- Argon2id client-secret verification runs synchronously on the async worker **while holding the tenant write lock** — attacker-triggerable (valid client_id + wrong secret) head-of-line blocking, bounded only by the 12/min/IP limiter (`src/oidc.rs:1797`, `src/idp.rs:251-257`).
- Ceremony caches capped at 10k entries (moka evicts under pressure): distributed flooding of the cheap `/authorize` park step can evict victims' pending login/consent states and parked codes within their 600-1800 s windows — fail-closed, availability-only (`src/oidc.rs:25-40`, `src/cache.rs:20-32`).
- `authorize_flow` drops and re-acquires the tenant guard mid-flow against DESIGN §6's explicit rule, and re-checks nothing after re-acquisition (unlike `authorize_resume`/`consent_submit`); downstream checks make it fail closed — impact limited to a wasted ceremony (`src/oidc.rs:1100-1119`).
- Introspection `username` returns the user UUID instead of a human-readable identifier (RFC 7662); the login name is resolvable — `/userinfo` does it (`src/oidc.rs:3609-3614,3684-3689` vs `:3198-3204`).

### Factors & user lifecycle
- Unauthenticated `passkey/request` is an account/passkey-existence oracle: 200 + challenge (incl. `allowCredentials` ids) for usernames with active passkeys, 401 otherwise — contradicts DESIGN.md:17's enumeration-resistance invariant; email/OTP `request` responses are correctly identical across signin/signup (`src/passkey.rs:469-483`).
- Social callback renders post-exchange failures (token exchange, missing subject, user creation, JWT mint) as HTTP **200** with internal error text — invisible to status-based monitoring (`src/social.rs:889-978`).
- TOTP step-up forces a re-enrollment round-trip: `totp/verify` requires a one-shot enrollment token; obtaining one for an ACTIVE record consumes a current code and re-returns the plaintext secret as `qr`+`uri` — a code-phishing proxy upgrades a one-time code into the permanent secret (`src/totp.rs:376-445,494-525`).
- MFA policy/ACR hardcode TOTP: `jwt.mfa.contains("totp") && len > 1` — a UV-required passkey or email+otp session can never satisfy `mfa=true` or reach ACR 2 (`src/policy.rs:188`, `src/db.rs:84`).
- Re-request does not invalidate previously issued ceremony secrets: up to 3 live magic links/min coexist; each OTP `request` mints a new code without revoking the prior (`src/email.rs:402-407`, `src/otp.rs:240-250`).
- No delivery-failure retry/fallback for email or SMS: single attempt then hard 401; user must re-request against the 3/min throttle (`src/email.rs:402-410`, `src/otp.rs:235-253`, `src/aliclient.rs:389-402`).
- OTP add-flow code generation is modulo-biased (`buf[0] % 10`, no rejection sampling — the login path does it correctly) and `otp/add_verify` has no attempt gate (`src/otp.rs:706-716,850-908`).
- Authenticated enumeration via add endpoints: "Email already in use" / "Mobile already in use" to any valid session (`src/email.rs:889-890`, `src/otp.rs:769-770`).

### Infra & ops
- Salvo `MokaStore` rate limiters are capacity-`u64::MAX` with no TTL: every distinct source IP ever observed retains an entry for the process lifetime — sustained IP rotation grows memory unboundedly (`src/router.rs:84-89,219-224,399-404`, `src/scim.rs:795-802`).
- `/api/v1/doc/openapi.json` + `/api/v1/doc/scalar` are unauthenticated on every tenant host — full API schema including admin endpoints exposed anonymously (`src/router.rs:483-492`).
- CORS `["tenant"]` share mode never matches: bare domain IDs compared against full `Origin` strings — dead as shipped, fail-closed (`src/db.rs:526-543`, `src/cors.rs:38-39`).
- Latent `rest[2..]` slice panic in the metrics route labeler for a path `/api/v1/auth/social` — unreachable today (no leaf route matches), but any future route there makes it a remotely triggerable per-connection panic (`src/ops.rs:275-278,354-357`).
- Boot-time `load_tenant` inserts the tenant into the map *before* registering domains; on a cross-tenant domain conflict it returns Err but the tenant stays loaded — half-loaded topology instead of a clean refusal (`src/db.rs:742-771,822-833`).
- `/admin/metrics` is process-global telemetry behind a per-tenant admin gate: any one tenant's admin observes cross-tenant aggregates (tenant counts, per-route counters, auth outcomes) (`src/ops.rs:402-419`).
- `seed.example.toml` registers `0.0.0.0` as a tenant domain (direct-IP requests resolve to the bootstrap tenant with its root/admin users — a pattern operators will copy) and carries a stale SECURITY note claiming an attach-on-first-verify takeover that the code now refuses (`seed.example.toml:32-35,50`).
- `.dockerignore` excludes `.env`/`janux.toml` but not `base.toml`/`seed.toml` — the local encryption key and seed credentials are sent to the Docker build context (never COPYed into the image).

### Frontend
- Discovery `identifier` factor metadata (`email`/`mobile`/`null`) declared, typed, never read — inputs hardcoded per factor; the UI cannot adapt to the documented contract (`src/oidc.rs:741,753,783`, `frontend/src/login/App.tsx:268-306`, docs/FRONTEND.md:113-115).
- No logout control in any app; `openapiVerifyLogout` exported, never imported; session lives in `sessionStorage` until tab close.
- Login "done" phase is a dead end: no `client_id`/`redirect_uri` in the URL → static "Signed in." with no navigation (`frontend/src/login/App.tsx:69-73,374`).
- Consent page can show only the raw `client_id` even though a friendlier `client_name` is storable via `oauth2client/meta` — users consent to an opaque identifier (`src/oidc.rs:327-330`, `src/oidc_ext.rs:803-811`).
- Device `user_code` input is trimmed but never case-folded — typing `abcd-1234` fails the exact-match lookup against uppercase RFC 8628 codes (`frontend/src/device/App.tsx:67,87`, `src/oidc.rs:3928-3930`).
- `?error=` query text rendered verbatim via a prototype-unsafe plain-object lookup: arbitrary attacker-chosen copy in the sign-in banner (React escapes it — no XSS), and `?error=constructor` renders an invalid React child (`frontend/src/login/App.tsx:25-31,54,243`).
- The SPA-consumed OIDC endpoints (`/authorize/resume`, `/consent*`, `/device-login/*`) live in `public_routes()`, excluded from the OpenAPI merge — hand-rolled untyped fetch with locally re-declared shapes; contract drift invisible to `just openapi` (`src/bin/openapi.rs:97-98`, `src/router.rs:405-470`).
- GET-with-required-body routes (`admin/user/roles`, `totp/enroll`, `totp/verify` GET variants) generate SDK functions that throw in browsers (`fetch` forbids GET bodies); POST variants work; no UI currently calls the broken ones (`frontend/openapi.json:3131-3142`, `src/api/client/client.gen.ts:83-89`).
- Embedded `frontend/dist/` is older than `frontend/src/` — a `cargo build` without a preceding `npm run build` ships a stale admin bundle (the exact drift FRONTEND.md:143-145 warns about).
- No form semantics anywhere: zero `<form>`/`onSubmit` — Enter never submits (login, device code entry, every admin create form); accessibility + password-manager workflows broken; `autoComplete="one-time-code"` hints at intent the missing `<form>` undercuts.
- All strings hardcoded English, no i18n hook (acceptable for an overridable default frontend, but zero translation surface); server scope labels English-only too.
- Admin console has no overflow handling on mobile: full-width 6-8-column tables, no scroll wrapper, only CSS is `body { margin: 0 }`.
- Dead code/deps: `public/icons.svg` referenced nowhere; `@hey-api/vite-plugin` unused in vite.config; `clearSession`/`loadSession` re-exports imported by nobody.
- Admin tables fetch fields they never render (key material, redirect URIs, provider client_id/scopes) — operators can't verify what's already on the wire.

### Docs / CI hygiene
- DESIGN.md:33 and INTEGRATION.md:128 builtin-role catalogs omit `scim` (level 60), contradicting `src/role.rs:22-28` and DESIGN §7 itself.
- Every code-line citation in INTEGRATION.md has drifted ~100-200 lines (9 verified examples: main.rs:48→86-95, utils.rs:312→400, email.rs:126→205, idp.rs:373→519, oidc.rs:1547→1709, jwt.rs:114→154, router.rs:71→84-89, …); behavioral claims still check out.
- `tests/README.md` references nonexistent just targets (`test-integration`, `test-e2e`, `test-e2e-headed`), a nonexistent `e2e_config::base_url()` helper, conflates the lib suite with the `tests/unit/` target, and omits `tests/compliant` entirely.
- `tests/compliant/README.md:35` quickstart uses a stale pre-extraction path (`auth/test/compliant`).
- `RUN_ENV`-keyed `config/{RUN_ENV}.toml` layer and `JANUX_CONFIG_FILE` are undocumented — silent override paths the README's precedence description doesn't mention (`src/server.rs:327,333`, `src/main.rs:86-92`).
- `build.rs` browser detection calls `fs::metadata()` on a literal `chromium-*` glob path — can never succeed, warning fires on every build; docstring describes commented-out behavior.
- FRONTEND.md:139 says `just dev` needs a separately running backend; `just dev` starts both (root README is correct).
- DESIGN.md:11 says provisioning happens at `request`; code provisions at `verify` — internally inconsistent with DESIGN.md:16.
- No SECURITY.md (disclosure policy for an IdP), no CONTRIBUTING/CHANGELOG/CODEOWNERS/rust-toolchain.toml/dependabot.

---

## ✅ Verified solid (no gap found)

- Magic-link single-use & binding: one-shot consume before validation, `iss`/`sub`/`data.email` checks, signin/signup mode fixed in the signed token and fail-closed for legacy tokens, namespace separation from add tokens.
- OTP attempt budgets: per-IP 6/min, per-recipient 3/min, per-account 5-failure exponential-backoff lockout checked *before* one-shot consumption; CSPRNG codes with rejection sampling on the login path; opaque flow handle (H1 fixed).
- TOTP replay: current-step-only match + consumed-step guard, one-shot enrollment token, secrets encrypted at rest with in-place legacy upgrade; TOTP cannot bootstrap a session.
- WebAuthn: random per-flow challenges (600 s TTL, one-shot), username binding on login flows (C1 fixed), origin/rp_id derived from trusted hosts, post-finish credential-ownership recheck, counter-regression enforcement.
- Social federation: state + PKCE S256 + nonce + `at_hash` all validated; email trusted only when `email_verified`; **no auto-merge** — provisioning creates a fresh user and skips emails owned by others; link is session-gated; login code one-shot + HttpOnly bind cookie.
- Factor-laundering prevention: prior factors inherited only from a same-user session across all five factors, each pinned by a regression test.
- Verify races: per-tenant write guard across the handler, atomic one-shot cache removal, all-or-nothing signup with rollback.
- Token validation: RS256-pinned (no alg confusion), kid-routed, revocation checked before decode, issuer/domain binding, refresh rotation single-winner with family poisoning (H4 fixed); persistent revocation store surviving restarts.
- Secrets at rest: AES-256-GCM with random nonces for signing keys (H2 fixed), TOTP, social, provider credentials; encryption key mandatory at boot. Residual: legacy plaintext fallback (G-150).
- Tenant isolation: per-tenant DB files, Host-derived resolution, issuer binding, domain-bound session tokens — the review-time exceptions have since been resolved or dispositioned: G-152 fixed (tenant-scoped send throttle), G-148 closed by design (domain registration is an operator trust decision, DESIGN.md §5), G-149 partially fixed (safe example default + boot warning; allowed-proxy validation still open).
- Default-deny route coverage: every route justified except the unauthenticated doc endpoints (Low, above).
- Login SPA open-redirect handling: same-origin resolution with protocol-relative/backslash evasion tests; server-provided redirects constructed from the parked, validated callback URI.
- Generated client ↔ spec ↔ backend in sync at review time (61 paths spot-checked); caveat: nothing enforces freshness in CI beyond the OpenAPI drift check on `router::api()`.
- Consent/device contract fidelity: pages match the backend wire contract exactly (limitations are server-side design, tracked above).

---

## Recommended fix order

1. **G-138** — the only open High: no TOTP UI means `mfa=true` policies are a dead end. Backend ceremonies (`totp/enroll|verify`) and generated SDK calls exist; the missing pieces are enrollment + step-up screens in the login/admin apps (and `janux_factors` advertisement).
2. **SCIM trio (G-144, G-145, G-146)** — unbounded list, `userName` case-sensitivity, PATCH `remove`/`add` semantics: RFC 7644 conformance, and prerequisite weight for G-124 (external IdP verification).
3. **G-143 + G-96** — OAuth2 client lifecycle: per-client `RedirectURI` keyspace + transactional creation unblocks delete+recreate rotation (G-96) and closes the DCR squatting vector; G-121's atomicity note applies to the same commit points.
4. **Frontend cluster (G-155, G-156, G-157, G-161, G-162, G-163, G-165)** — handle both 403 signals (`X-MFA-Required`, `X-Reauth-Required`), confirmation dialogs, cookie-era refresh (server side is ready since G-139), TS `strict`, self-service credential management, deny-policy visibility, browser-driven coverage.
5. **Remaining backend mediums** — G-153 (network sends under the tenant write guard), plus the G-149/G-150 residuals (allowed-proxy validation; key versioning/rotation tooling).
6. **Legacy opens by impact** — G-106 (session visibility/kill; pairs with the now-closed G-128/G-151), G-126 (durable back-channel retry), G-121, G-125, G-124, G-93, G-99/G-100/G-101, G-105, G-112, and the G-28/G-122/G-127 residuals; audit v2 residuals track as G-166.
7. **Low tail** — triage and assign IDs as items are promoted.
